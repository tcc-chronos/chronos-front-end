export interface LogoProps {
  /**
   * Tamanho do logo
   */
  size?: 'sm' | 'md' | 'lg' | 'xl';
  /**
   * Se deve mostrar o texto "Chronos" junto com o logo
   */
  showText?: boolean;
  /**
   * Classes CSS adicionais
   */
  className?: string;
}
